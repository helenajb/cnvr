extends layout

block content
    h1.
        User List in host XXXX
    ul
        each user, i in userlist
            li #{user.username} : #{user.email}
    a(href="/newuser")
        button(type="button") Add user
